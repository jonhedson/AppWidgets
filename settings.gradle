pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "AppWidgets"
include ':app'
include ':app:apptextview'
include ':app:appdynamictextview'
include ':app:appautocompletetextview'
include ':app:appdynamicautocompletetextview'
